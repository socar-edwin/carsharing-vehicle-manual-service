# 차종별 매뉴얼 챗봇 - Vector Store 설계

## 1. 데이터 현황

| 항목 | 값 |
|------|-----|
| 총 차종 | 105개 |
| 차량 유형 | 5개 (경형, 준중형/중형, 대형, SUV, 전기차) |
| body 평균 길이 | 3,477자 |
| body 최대 길이 | 6,025자 |

## 2. Vector Store 설계 옵션 비교

### 옵션 A: 단일 컬렉션 + 메타데이터 필터링 (권장)

```
Collection: vehicle_manuals
├── Document 1: 아이오닉 5 매뉴얼 (청크 1)
│   └── metadata: {vehicle_name: "아이오닉 5", vehicle_type: "전기차", section: "시동"}
├── Document 2: 아이오닉 5 매뉴얼 (청크 2)
│   └── metadata: {vehicle_name: "아이오닉 5", vehicle_type: "전기차", section: "충전"}
├── Document 3: 캐스퍼 매뉴얼 (청크 1)
│   └── metadata: {vehicle_name: "캐스퍼", vehicle_type: "경형", section: "시동"}
└── ...
```

**장점:**
- 관리 간단 (인덱스 1개)
- ChromaDB/Qdrant 메타데이터 필터링 효율적
- 필요시 차종 간 유사 검색 가능
- 업데이트/삭제 용이

**단점:**
- 대규모 데이터시 필터링 오버헤드 (현재 규모에선 무시 가능)

### 옵션 B: 차량 유형별 컬렉션 (5개)

```
Collection: vehicles_electric  (전기차 20개)
Collection: vehicles_suv       (SUV 32개)
Collection: vehicles_midsize   (준중형/중형 22개)
Collection: vehicles_large     (대형 20개)
Collection: vehicles_compact   (경형 11개)
```

**장점:**
- 유형별 검색 빠름

**단점:**
- 컬렉션 관리 복잡
- 유형 변경시 재인덱싱 필요

### 옵션 C: 차종별 개별 컬렉션 (105개)

**장점:**
- 완전한 격리

**단점:**
- 관리 매우 복잡
- 메모리 비효율
- 신규 차종 추가시 컬렉션 생성 필요

## 3. 권장 설계: 옵션 A (단일 컬렉션 + 메타데이터 필터링)

### 3.1 청킹 전략

body 내용을 섹션별로 청킹하여 검색 정확도 향상:

```python
# 청킹 기준 (body 내 섹션 구분자)
section_markers = [
    "시동 걸기", "기어 조절", "파킹 브레이크",
    "충전", "주유", "블루투스", "트렁크",
    "에어컨", "시트 조절", "사이드 미러"
]

# 예시: 아이오닉 5 body → 여러 청크로 분리
chunk_1 = "[아이오닉 5] 시동 걸기: 브레이크를 밟고 시동 버튼을 누르세요..."
chunk_2 = "[아이오닉 5] 충전 방법: 충전카드는 대시보드 앞 리더기에..."
chunk_3 = "[아이오닉 5] 블루투스 연결: 설정 > 블루투스에서..."
```

### 3.2 메타데이터 스키마

```python
metadata = {
    # 필수
    "vehicle_name": str,      # "아이오닉 5"
    "vehicle_type": str,      # "전기차"

    # 검색 필터용
    "section": str,           # "충전", "시동", "블루투스" 등

    # 참조용
    "source_id": str,         # 원본 document ID
    "html_url": str,          # 원본 웹 URL
    "updated_at": str,        # 업데이트 일시
}
```

### 3.3 검색 파이프라인 (현재 구현)

```
┌─────────────────────────────────────┐
│  ① 차종 + 질문 입력                  │  UI에서 선택/입력
│  vehicle_name, query               │
└────────────┬────────────────────────┘
             ▼
┌─────────────────────────────────────┐
│  ② Query Analysis (LLM)            │  ← Pre-Retrieval
│  - is_service_inquiry: 서비스 문의?  │
│  - keywords: 구어체→공식 키워드      │
│  - is_action_query: 대처법 질문?    │
└────────────┬────────────────────────┘
             ▼
┌─────────────────────────────────────┐
│  ③ 차종 매뉴얼 로드                  │
│  - Metadata Filter (vehicle_name)  │
│  - Query-based Vector Search       │
│  - 관련 청크 우선 로드 (15개)        │
└────────────┬────────────────────────┘
             ▼
┌─────────────────────────────────────┐
│  ④ Query Routing                   │
│  is_service_inquiry 판단            │
│  ┌─────────────────────────────┐   │
│  │ true  → Help Center 추가 검색 │   │
│  │ false → 매뉴얼만 사용        │   │
│  └─────────────────────────────┘   │
└────────────┬────────────────────────┘
             ▼
┌─────────────────────────────────────┐
│  ⑤ 컨텍스트 구성                     │
│  - 매뉴얼 청크 전체 사용             │
│  - 매뉴얼 + 도움말 컨텍스트 결합     │
└────────────┬────────────────────────┘
             ▼
┌─────────────────────────────────────┐
│  ⑥ LLM 답변 생성 (GPT-4o-mini)     │
│  컨텍스트 기반 자연어 답변            │
└────────────┬────────────────────────┘
             ▼
┌─────────────────────────────────────┐
│  ⑦ Source Selection (LLM)          │  ← Post-Retrieval
│  가장 적절한 출처 문서 선택           │
└────────────┬────────────────────────┘
             ▼
┌─────────────────────────────────────┐
│  ⑧ 답변 + 출처 반환                  │
│  - answer, sources, help_center_sources│
│  - URL + 텍스트 하이라이트 (#:~:text=)│
└─────────────────────────────────────┘
```

### 3.4 Help Center 연동

차량 매뉴얼 외 서비스 관련 질문(예약, 취소, 사고 등)은 Help Center도 함께 검색:

```
질문: "사고가 나면 어떻게 해야 하나요?"
         ↓
Query Analysis: is_service_inquiry = true
         ↓
┌─────────────────────────────────────┐
│  차량 매뉴얼 검색                     │
│  + Help Center Hybrid Search        │
│    (BM25 + Vector + RRF)            │
└─────────────────────────────────────┘
         ↓
답변: 매뉴얼 + Help Center 내용 통합
```

### 3.5 URL 텍스트 하이라이트

출처 URL에 Chrome Text Fragment 기능 적용:

```
기존: https://socar-docs.zendesk.com/hc/ko/articles/123456
변경: https://socar-docs.zendesk.com/hc/ko/articles/123456#:~:text=주유구

→ 페이지 로드 시 "주유구" 텍스트가 하이라이트되고 해당 위치로 스크롤
```

## 4. 구현 계획

### 4.1 디렉토리 구조

```
vehicle-info-chatbot/
├── src/
│   ├── vehicle_data_loader.py    # 차종 데이터 로딩 + 청킹
│   ├── vehicle_indexing.py       # Vector Store 인덱싱
│   ├── vehicle_retriever.py      # 메타데이터 필터 검색
│   └── vehicle_chatbot.py        # LLM 답변 생성
├── api/
│   └── vehicle_chat_api.py       # 챗봇 API
├── app_vehicle.py                # Streamlit 차종 챗봇 UI
└── data/
    └── vehicle_manual_data.csv   # 105개 차종 데이터
```

### 4.2 핵심 코드 구조 (현재 구현)

```python
# vehicle_retriever.py
class VehicleRetriever:
    def __init__(self, index, default_top_k=5):
        self.index = index
        self.default_top_k = default_top_k

    def get_vehicle_document(
        self,
        vehicle_name: str,
        query: str = "",
        max_chunks: int = 20
    ) -> List[Dict]:
        """
        차종 매뉴얼 로드 (쿼리 기반 관련 청크 우선)
        - Metadata Filter로 차종 필터링
        - Query-based Vector Search로 관련 청크 우선 로드
        """
        metadata_filters = MetadataFilters(
            filters=[MetadataFilter(key="vehicle_name", value=vehicle_name)]
        )
        retriever = VectorIndexRetriever(
            index=self.index,
            similarity_top_k=max_chunks,
            filters=metadata_filters
        )
        nodes = retriever.retrieve(query if query else "차량 매뉴얼 정보")
        return [format_node(node) for node in nodes]
```

```python
# vehicle_chatbot.py
class VehicleChatbot:
    def __init__(self, retriever, help_center_retriever=None, api_key=None):
        self.retriever = retriever
        self.help_center_retriever = help_center_retriever  # Help Center 연동
        self.client = OpenAI(api_key=api_key)

    def chat(self, query: str, vehicle_name: str) -> Dict:
        """차종별 질문 응답 (Help Center 연동)"""

        # 1. Query Analysis (Pre-Retrieval)
        analysis = self._analyze_query(query)

        # 2. 차종 매뉴얼 로드 (쿼리 기반 관련 청크 우선)
        vehicle_chunks = self.retriever.get_vehicle_document(
            vehicle_name=vehicle_name,
            query=query,
            max_chunks=15
        )

        # 3. Query Routing - 서비스 문의시 Help Center 추가 검색
        if self._is_general_inquiry(query, analysis):
            help_center_results = self.help_center_retriever.search(query)

        # 4. 컨텍스트 구성 + LLM 답변 생성
        answer = self._generate_answer(vehicle_chunks, help_center_results, query)

        # 5. Source Selection (Post-Retrieval) + URL 텍스트 하이라이트
        sources = self._build_sources_with_highlight(vehicle_chunks, query)

        return {"answer": answer, "sources": sources, ...}
```

### 4.3 Streamlit UI 구조

```
┌────────────────────────────────────────────┐
│  🚗 차량 매뉴얼 챗봇                         │
├────────────────────────────────────────────┤
│                                            │
│  [차량 유형 선택] ▼  전기차                  │
│                                            │
│  [차종 선택] ▼  아이오닉 5                   │
│                                            │
├────────────────────────────────────────────┤
│                                            │
│  💬 채팅                                    │
│  ┌────────────────────────────────────┐   │
│  │ 🧑 충전은 어떻게 하나요?              │   │
│  └────────────────────────────────────┘   │
│  ┌────────────────────────────────────┐   │
│  │ 🤖 아이오닉 5의 충전 방법입니다:       │   │
│  │                                    │   │
│  │ 1. 대시보드 앞 리더기에 있는 쏘카     │   │
│  │    전기충전 카드를 사용하세요.        │   │
│  │ 2. 충전기 화면에서 '충전 시작'을      │   │
│  │    선택합니다.                       │   │
│  │ ...                                │   │
│  │                                    │   │
│  │ 📎 출처: [원본 매뉴얼 보기](url)     │   │
│  └────────────────────────────────────┘   │
│                                            │
│  [질문을 입력하세요...]              [전송] │
│                                            │
└────────────────────────────────────────────┘
```

## 5. 예상 비용 (월 10,000 쿼리 기준)

| 항목 | 비용 |
|------|------|
| 임베딩 (인덱싱, 1회) | ~$0.50 |
| 임베딩 (쿼리) | ~$0.10 |
| Claude 3.5 Sonnet (답변 생성) | ~$15.00 |
| **총 예상 비용** | **~$15.60/월** |

## 6. 구현 상태

1. [x] `vehicle_data_loader.py` 구현 (청킹 로직)
2. [x] `vehicle_indexing.py` 구현 (Vector Store 생성)
3. [x] `vehicle_retriever.py` 구현 (메타데이터 필터 + 쿼리 기반 검색)
4. [x] `vehicle_chatbot.py` 구현 (LLM 답변 생성 + Help Center 연동)
5. [x] `app_vehicle.py` 구현 (Streamlit UI)
6. [x] Query Analysis (Pre-Retrieval) 추가
7. [x] Query Routing (차량/서비스 문의 분기) 추가
8. [x] Source Selection (Post-Retrieval) 추가
9. [x] URL 텍스트 하이라이트 기능 추가

## 7. 향후 개선 사항

- [ ] Evaluation pipeline (Precision@5, Recall@5, MRR)
- [ ] 사용자 피드백 수집 및 분석
- [ ] 응답 캐싱 (자주 묻는 질문)
- [ ] 다국어 지원
